name: Build ZMK firmware

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.16.0
    with:
      build_matrix: |
        [
          {
            "board": "nice_nano_v2",
            "shield": "breeze_left"
          },
          {
            "board": "nice_nano_v2",
            "shield": "breeze_right"
          }
        ]
